from distutils.core import setup

setup(
    name='ProbablisticKNN',
    packages=['ProbablisticKNN'],
    version='0.1',
    description='A classifier and a probablistic model both using the K-Nearest-Neighbors algorithm',
    author='Joshua Reeve',
    author_email='joshua.reeve@ngc.com',
    keywords=['KNN', 'K', 'Nearest', 'Neighbors', 'Algorithm', 'classifier', 'probablistic', 'model'],
    classifiers=[]
)
